Backend: 
    Install all required python packages using `pip install -r backend/requirements.txt`<br/>
    For the node packages, use `npm install`. This will read the package.json file for required packages. <br/>
    To run, use command `python -m backend.api.main`